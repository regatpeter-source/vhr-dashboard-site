# Fill and rename to .env to enable PostgreSQL + user sync
DATABASE_URL=postgres://USER:PASS@HOST:5432/DBNAME
SYNC_USERS_SECRET=changeme-sync-secret
ADMIN_INIT_SECRET=changeme-admin-init
# HTTPS_ENABLED=1
# FORCE_HTTP=0
