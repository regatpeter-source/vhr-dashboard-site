/* Minimal botpress config stub for webchat (to avoid 404 and configure widget) */
window.botpressWebChat = window.botpressWebChat || {};
window.botpressWebChat.botId = window.botpressWebChat.botId || 'vhr';
window.botpressWebChat.host = window.botpressWebChat.host || 'https://cdn.botpress.cloud';
/* Additional config may be placed here: window.botpressWebChat.config = { ... } */
