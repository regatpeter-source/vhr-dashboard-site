# Exemple .env pour usage local (client final)
# Auth et démo 7 jours restent inchangées

# Forcer HTTP pour ADB local
FORCE_HTTP=1
# Masquer les logs info pour l’utilisateur final
QUIET_MODE=1
# En prod locale
NODE_ENV=production

# SMTP (facultatif en local, mais laissé pour compatibilité)
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=
BREVO_SMTP_PASS=
EMAIL_FROM=noreply@vhr-dashboard.com
EMAIL_ENABLED=true

# Stripe (laisser vide si non utilisé en local)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
