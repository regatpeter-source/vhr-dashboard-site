# Copiez ce fichier en .env sur la machine cliente pour que le dashboard PRO utilise PostgreSQL 18.
# Renseignez les valeurs marquées <...>. Utilisez un utilisateur Postgres restreint (auth uniquement),
# et limitez le firewall/SG au couple IP:port du client. Activez SSL/TLS.

NODE_ENV=production
PORT=3000
PUBLIC_BASE_URL=https://localhost:3000
API_BASE_URL=https://localhost:3000

# Base de données Postgres 18 (obligatoire)
DATABASE_URL=postgresql://<pg_user_restreint>:<motdepasse_fort>@<hote_pg>:5432/<base_pg>
DATABASE_SSL=true
PGSSLMODE=require

# Sécurité application
JWT_SECRET=<jwt_secret_client_unique>
LICENSE_SECRET=<license_secret_client_unique>
NO_ADB=0
QUIET_MODE=1
FORCE_HTTP=0
HTTPS_ENABLED=1
HTTPS_CERT_FILE=./cert.pem
HTTPS_KEY_FILE=./key.pem
# HTTPS_CA_FILE=./fullchain.pem

# Stripe désactivé côté client (laisser vide)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email désactivé côté client
EMAIL_ENABLED=false
SMTP_USER=
SMTP_PASS=
SMTP_HOST=
SMTP_PORT=587
EMAIL_FROM=noreply@example.local

# SQLite n’est pas utilisé si DATABASE_URL est défini, mais on laisse un chemin par défaut
DB_SQLITE_FILE=./data/vhr.db
